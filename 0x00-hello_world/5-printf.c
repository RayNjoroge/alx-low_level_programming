/*include the io standard header*/
#include <stdio.h>

/**
 * main - this is the entry point.
 * printf  - prints the required string of text.
 * Return: - returns value 0.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
